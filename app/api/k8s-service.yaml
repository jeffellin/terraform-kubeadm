apiVersion: v1
kind: Service
metadata:
  name: wordpress-stats-api
  namespace: wordpress
  labels:
    app: wordpress-stats-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: wordpress-stats-api
